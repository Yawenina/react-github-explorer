import React from 'react';
import PropTypes from 'prop-types';

const MenuOpenHandler = props => {
  return (
    <div>
      MenuOpenHandler
    </div>
  );
};

MenuOpenHandler.propTypes = {
  
};

export default MenuOpenHandler;