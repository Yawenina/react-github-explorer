import React from 'react';
import PropTypes from 'prop-types';

const UserPage = props => {
  return (
    <div>
      UserPage
    </div>
  );
};

UserPage.propTypes = {
  
};

export default UserPage;