import React from 'react';
import PropTypes from 'prop-types';

const RepoItem = props => {
  return (
    <div>
      RepoItem
    </div>
  );
};

RepoItem.propTypes = {
  
};

export default RepoItem;